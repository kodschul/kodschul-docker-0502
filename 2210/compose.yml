services:
  frontend:
    build: frontend
    ports:
      - 8000:80

    volumes:
      - ./frontend:/app

  backend:
    build: backend
    ports:
      - 8002:80

  web:
    image: nginx:latest
    ports:
      - 8010:80
