services:
  frontend:
    build: frontend
    ports:
      - 8000:80

    # volumes:
    #   - ./frontend:/app

    develop:
      watch:
        - path: ./frontend
          action: rebuild

  backend:
    build: backend

  web:
    image: nginx:latest
    ports:
      - 8010:80
