apiVersion: v1
kind: Service
metadata:
  name: frontend

spec:
  selector:
    app: frontend

  ports:
    - port: 80 # WOHER komme ich?
      nodePort: 31500
      targetPort: 80 # WOHIN DAMIT ?

  type: NodePort
